# AdaBCL
To change parameters of MNIST test, go to main() function and add the required parameters to the custom optimizer object initialization.
To change parameters in Quadratic tests, go to optizer list made before each test and change the parameters of the function in both cells
